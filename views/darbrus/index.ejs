<% include ../partials/header %> 

 <div class="container">
     <header class="jumbotron">
         <div class="container">
             <h1>Transporto valdymo modulis!</h1>
             <p>Darbu rusis</p>
             <p>
                <a class="btn btn-primary btn-large" href="/darbrus/new">Nauja darbo rusis</a>
             </p>
         </div>
     </header>
     
     <div class="row text-center" style="display:flex; flex-wrap: wrap;">
        <% alldarbrus.forEach(function(alldarbrus){ %>
            <div class="col-md-3 col-sm-6">
                   <div class="caption">
                       <h4><%= alldarbrus.darbrus %></h4>
                       <h4><%= alldarbrus.vnt %></h4>
                       <div>
                           <hr>
                           <a class="btn btn-xs btn-warning" href="/darbrus/<%= alldarbrus._id%>/edit">REDAGUOTI</a>
                           <hr>
                           <form id="delete-form" action="/darbrus/<%=alldarbrus._id%>?_method=DELETE" , method=POST>
                           <button class="btn btn-xs btn-danger">TRINTI</button>
                           </form>
                       </div>
                   </div>
            </div>
        <% }); %>
    </div>
</div>

<% include ../partials/footer %>
